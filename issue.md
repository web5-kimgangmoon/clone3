## 이슈발생

1. 헤더 애니메이션, 탑일 경우와 탑이 아닐 경우에 따라 헤더가 달라진다. 그리고 애니메이션이 이 두 가지 경우의 요소의 위치가 달라진다.

2. width값이 없이 ellipsis 속성을 써 말줄임표를 쓰고 있었다.

## 경과

1. 애니메이션과 각각 기준 위치가 달라지는 요소를 구현하고자 했다.

   1. 한 요소를 움직이며, 이 두 가지 경우에 relative 기준요소가 달라지게 애니메이션을 넣어봤다.

      - 실패: 애니메이션이 부자연스러워졌다.

   2. 두 요소를 만들고. 흐려졌다가 선명해지는 애니메이션으로 마치 한 요소가 이동한 것처럼 만들었다.

      - 실패: 마지막에 부자연스럽게 위치가 변경됐다.

2. 원본 페이지를 열심히 뒤져봤다. 그러나 거기서도 부모 요소의 값을 찾을 수 없었다.

## 해결

1. 두번째 해결책에서 opacity를 포지션 변경 애니메이션 중간에 서로 끝났다가 이어지게 만들어줌으로써 마치 한 요소가 움직이는 것처럼 보여줬다.

2. chatgpt도 뒤져보고, 스스로도 고민하고, 실제로 써보면서 이것저것 만져본 덕에 이런 결론을 냈다.

   - grid-template-columns는 일괄적용을 위해 사용했다.

   - min-width와 max-width에 의해 계산된 width 값은 해당 요소에서는 유동적인 값처럼 여겨지지만, 자식 요소에서는 계산된 최종값을 기준값으로 여긴다.

   - flex는 position 조절을 위해, 내부에 span이 들어있는데도 div처럼 동작하는 이유는 flex의 자식속성들의 기본값, grow 0, shrink 1, basis auto에 의해서 기본값들이 무시되어 계산된다.
